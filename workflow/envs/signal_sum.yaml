channels:
dependencies:
  - python=3.10
  - samtools>=1.17  # Adjust to the closest compatible version
  - pip
  - pip:
      - lib-pod5==0.3.21
      - numpy<2.0
      - pysam==0.22.1
      - tqdm==2.2.3
