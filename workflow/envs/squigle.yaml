name: sort
channels:
  - conda-forge
  - bioconda
dependencies:
  - python = 3.10
  - numpy = 1.26.4
  - pip
  - pip:
    # currently not working with numpy v2.0
    - pyslow5==0.2.0a1
    - squigualiser==0.6.3