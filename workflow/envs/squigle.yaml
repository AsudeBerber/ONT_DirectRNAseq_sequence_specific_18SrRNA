name: sort
channels:
  - conda-forge
  - bioconda
dependencies:
  - python = 3.10
  - pip:
    # currently not working with numpy v2.0
    - numpy==1.20.3
    - Cython # needed to make squigualiser run
    - pyslow5==0.2.0a1
    - squigualiser==0.6.3