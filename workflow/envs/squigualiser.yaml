name: squigualiser
channels:
  - conda-forge
  - bioconda
dependencies:
  - pip
  - pip:
    - cython==3.0.10
    - pyslow5==1.1.0
    - squigualiser==0.6.3
    - numpy < 2.0.0 #not working with numpy v2.0